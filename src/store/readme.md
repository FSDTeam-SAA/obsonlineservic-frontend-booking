# Global State Store

Contains global Zustand stores that manage application-wide UI and shell states (e.g. `ui.store.ts` for sidebar and loading overlays). Feature-specific states should remain localized to feature contexts/stores.
