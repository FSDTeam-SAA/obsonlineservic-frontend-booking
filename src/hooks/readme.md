# Global Reusable Hooks

This directory contains hooks that are shared across multiple features. Feature-specific hooks should reside in their respective feature folders (`src/features/{feature-name}/hooks/`).
